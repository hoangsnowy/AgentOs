{
  "//": "Production posture for the Web host. In the cloud the AppHost runs the Web as Production, so this file applies. Base appsettings.json keeps RequireDatabase=false for the standalone Development run (no DB); production must NOT silently fall back to no-op repositories — a missing/misnamed connection string should fail loudly rather than lose data.",
  "Persistence": {
    "RequireDatabase": true
  },
  "Auth": {
    "Keycloak": {
      "RequireHttpsMetadata": "true"
    }
  }
}
